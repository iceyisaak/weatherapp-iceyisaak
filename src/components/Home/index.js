import SearchBar from '../SearchBar';
import WeatherDisplay from '../WeatherDisplay';

const Home = () => {
  return (
    <div>
      <h1>Weatherapp</h1>
      <SearchBar />
      <WeatherDisplay />
    </div>
  );
};

export default Home;
